describe('math tests', () => {
  it('should add some numbers', () => {
    expect(1 + 1).toEqual(2)
  })

  it('should multiply numbers', () => {
    expect(10 * 2).toEqual(20)
  })
})

describe('advanced math tests', () => {
  it('should divide numbers', () => {
    expect(10 / 2).toEqual(5)
  })
})
